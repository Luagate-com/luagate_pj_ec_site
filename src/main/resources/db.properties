# ローカル開発用の初期値（PostgreSQL）。
# 本番環境では DB_URL / DB_USER / DB_PASSWORD を環境変数で設定する。
db.url=jdbc:postgresql://localhost:5432/ec_site
# 例（Neon Managed）: db.url=jdbc:postgresql://<host>/<database>?sslmode=require

db.user=ec_user
db.password=ec_password
