# Codespaces/ローカルで Tomcat 実行時に読み込む DB 接続情報
# このファイルをコピーして .env を作り、Neon の値で書き換えること
#   cp .env.example .env
# .env は .gitignore 済み

DB_URL=jdbc:postgresql://<your-host>.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
DB_USER=neondb_owner
DB_PASSWORD=<your-password>
