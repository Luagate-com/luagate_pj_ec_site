# Cloud Run デプロイ時に使う DB 接続情報
# このファイルを .env.deploy にコピーして実値を書き、deploy.sh を実行する
# 本ファイル(.example)はコミットして良い。.env.deploy は .gitignore 済み

# 例（Neon Managed Postgres）
DB_URL=jdbc:postgresql://<your-host>.neon.tech/<your-db>?sslmode=require
DB_USER=<your-user>
DB_PASSWORD=<your-password>
